[tool.poetry]
name = "poetry-1"
version = "0.1.0"
description = ""
authors = ["Loskutova"]
readme = "README.md"
packages = [{include = "poetry_1"}]

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
